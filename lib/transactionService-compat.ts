// Default export: forward the entire module (moved from lib/appwriteTransactionService.ts)
import * as _txn from './transactionService';

// Compatibility wrapper: re-export all named exports from the neutral transactionService
export * from './transactionService';

export default _txn;
